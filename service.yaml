apiVersion: v1
kind: Service
metadata:
  labels:
    app: udagram-feed-api
  name: udagram-feed-api
spec:
  ports:
    - port: 8000
      protocol: TCP
  selector:
    app: udagram-feed-api
  sessionAffinity: None
  type: ClusterIP
status:
  loadBalancer: {}
